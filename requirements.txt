# Plant disease Streamlit app + notebook
#
# Run the app (activate the venv first):
#   source .venv/bin/activate
#   streamlit run streamlit/main.py
# Copy your trained Keras file to: streamlit/trained_model.keras
#
# Streamlit Community Cloud: the deployment MUST use Python 3.11 or 3.12
# (set in the Cloud “Advanced settings” / app Settings — NOT via runtime.txt).
# The builder may default to 3.14; TensorFlow has no wheels for 3.14, so
# install fails with “No matching distribution” until you change the Python version.
streamlit>=1.28.0
tensorflow==2.17.1
numpy>=1.26,<3
pandas
Pillow
