# Plant disease Streamlit app + notebook
#
# Run the app (activate the venv first):
#   source .venv/bin/activate
#   streamlit run streamlit/main.py
# Copy your trained Keras file to: streamlit/trained_model.keras
#
# Streamlit Community Cloud: use Python 3.11 or 3.12 in the deploy “Advanced
# settings” dialog. TensorFlow has no wheels for 3.14+ yet, so 3.14 will fail
# to resolve dependencies.
streamlit>=1.28.0
tensorflow>=2.13
numpy
pandas
Pillow
